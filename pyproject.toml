[project]
name = "vtk-python-examples"
version = "0.1.0"
description = "VTK Python example corpus and gallery website (split from vtk-ontology)."
requires-python = ">=3.12"
dependencies = []

[tool.setuptools]
packages = ["scripts"]
